import {StyleSheet} from 'react-native';
import { pageBg } from '../../constants/styles/colors';

const styles = StyleSheet.create({
  scrollView: {
    flex: 1,
    backgroundColor: pageBg,
    marginTop: 48,
  },
 
});
export default styles;
